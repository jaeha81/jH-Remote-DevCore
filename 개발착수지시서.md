디스코드 음성 기반 개발 제어 시스템을 Codex로 구축한다.

핵심 구성은 2개 봇이다.

1. Discord Voice Agent Bot
- 디스코드에서 사용자의 음성 메시지 또는 음성 채널 발화를 수집한다.
- Whisper 또는 동급 STT 엔진으로 음성을 텍스트로 변환한다.
- 변환된 내용을 명령, 질문, 회의, 승인 요청, 피드백으로 분류한다.
- 정리된 텍스트를 Local Control Agent Bot으로 전달한다.

2. Local Control Agent Bot
- 로컬 PC의 개발 환경을 제어한다.
- 기본 프로젝트 저장 위치는 로컬 PC의 AI_Projects 폴더로 한다.
- 새 개발 착수 시 폴더 생성, GitHub 레포 생성, 초기 파일 구조 생성까지 담당한다.
- Codex를 메인 오케스트레이터로 두되, Claude Code도 코딩·디자인·분석에 적극 활용한다.
- 기존 로컬 시스템, Claude Code 설정, Codex 설정, Obsidian 동기화 구조는 절대 삭제하거나 무시하지 않는다.
- 작업 결과는 GitHub에는 코드로 저장하고, Drive에는 데이터/문서/리소스로 저장한다.
- 세션 종료 시 작업 로그, 요약, 결정사항, 다음 작업을 Markdown으로 정리해 Obsidian에 저장한다.
- Obsidian에서는 노드, 그래프 뷰, LLM Wiki, Daily Board 구조와 연결되도록 한다.

운영 규칙:
- 불필요하게 컨텍스트를 낭비하지 않는다.
- 본질을 벗어난 압축 요약을 하지 않는다.
- 기존에 설정된 Caveman Skill을 반드시 활용한다.
- Goal Mode가 필요한 경우에는 반드시 사용자 승인 후 사용한다.
- 디자인 작업은 AI가 만든 티가 나지 않도록 고품질로 구현한다.
- 디자인 방향은 작업 중 사용자 확인을 받고 진행한다.
- 디스코드에는 진행 상황, 오류, 승인 요청, 이미지/웹 미리보기, 작업 결과를 실시간으로 보고한다.

최종 목표:
사용자가 모바일 디스코드에서 음성으로 개발 아이디어를 말하면,
Voice Agent Bot이 이를 텍스트화하고,
Local Control Agent Bot이 로컬의 Codex와 Claude Code를 조율하여
프로젝트 생성 → 개발 착수 → 검수 → 저장 → Obsidian 지식화까지 이어지는
음성 기반 원격 개발 착수 시스템을 구축한다.